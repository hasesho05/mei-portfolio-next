# このリポジトリについて

写真家 Takahashi Mei のポートフォリオサイト(Next.js / TypeScript / pnpm)。

## 作業の入口

- **作品・写真・文言の追加や変更**(いちばん多い作業):
  サイトに載る中身はすべて `content/` フォルダにある。
  `docs/content-guide.md` を最初に読み、その手順とルールに従うこと。
  触るのは `content/` の中だけ。`src/` のコードは触らない。
- **コードの変更**: `AGENTS.md` がこのリポジトリの実装規約の唯一の正。
  必ず読んでから作業すること。

## 必ず守ること

- main ブランチで直接作業しない。ブランチを切って Pull Request を作る
- `content/` を変えたら `pnpm check` と `pnpm build` を実行し、
  両方通ってから完了とする(内容の間違いは日本語のエラーで表示される)
- 動画ファイルとカメラの元データ(巨大な画像)はコミットしない
